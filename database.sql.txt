-- TABEL PRODUK
CREATE TABLE product (
    id SERIAL PRIMARY KEY,
    nama_barang VARCHAR(255),
    harga INTEGER,
    stok INTEGER
);

-- TABEL TRANSAKSI
CREATE TABLE transaction (
    id SERIAL PRIMARY KEY,
    total_harga INTEGER,
    tanggal TIMESTAMP
);